# IANoticias — dependências enxutas (limite de bundle 500MB na Vercel, sem LangChain)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
openai==1.59.6
feedparser==6.0.11
trafilatura==2.0.0
pillow==11.1.0
httpx==0.28.1
jinja2==3.1.5
python-dotenv==1.0.1
python-multipart==0.0.20
itsdangerous==2.2.0
# Storage do Supabase é acessado via REST API (httpx) — não precisamos do
# supabase-py, que ainda valida a chave como JWT e rejeita as chaves novas.
